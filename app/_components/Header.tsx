import { Section } from "lucide-react"

export const Header = () => {
    return (
        <header>
            <Section>
                <h1 className="text-4xl font-bold text-primary-foreground">Abonnez-vous</h1>
                <p className="text-lg text-muted">
                    Web developper
                </p>
            </Section>
        </header>
    )
}